# Dependencies for bert_slang_filter.py (slang sense classification).
# Install torch CPU-only first to avoid the 2 GB CUDA default:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements_bert_slang_filter.txt
torch>=2.0.0
transformers>=4.30.0
pyyaml>=6.0
